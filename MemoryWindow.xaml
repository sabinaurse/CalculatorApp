<Window x:Class="CalculatorApp.MemoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Memory" Height="300" Width="250">
    <Grid>
        <Button Content="Închide" Width="80" Height="30" HorizontalAlignment="Center" 
        VerticalAlignment="Bottom" Margin="10" Click="CloseButton_Click"/>
        <ListBox x:Name="MemoryListBox" ItemsSource="{Binding MemoryList}" 
                 Margin="10" SelectionChanged="MemoryListBox_SelectionChanged"/>
    </Grid>
</Window>
